spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/waypoint
    username: waypoint
    password: waypoint
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

